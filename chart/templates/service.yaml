apiVersion: v1
kind: Service
metadata:
  name: {{ include "aws-eks-gitops-pipeline.fullname" . }}
  labels:
    {{- include "aws-eks-gitops-pipeline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aws-eks-gitops-pipeline.selectorLabels" . | nindent 4 }}
